node_modules/
**/node_modules/
.env
.env.*
!.env.example
!.env.sample
**/.env
!**/.env.example
!**/.env.sample

# Solana keypairs / secrets
*-mint.json
*-program.json
solana/devnet-mint.json

# Foundry
evm/out/
evm/cache/
evm/broadcast/
evm/lib/
evm/foundry.lock

# Anchor / Solana build
solana/target/
solana/.anchor/

# RPC overrides (API keys)
overrides.json
**/overrides.json
!**/example-overrides.json

# Upstream NTT CLI clone (re-bootstrap with `ntt new`)
ntt-workspace/

# OS
.DS_Store
*.log
